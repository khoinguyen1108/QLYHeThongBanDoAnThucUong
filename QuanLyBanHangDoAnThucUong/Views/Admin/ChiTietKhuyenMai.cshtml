@model QuanLyBanHangDoAnThucUong.Models.Entities.ChuongTrinhKhuyenMai
@{
    ViewData["Title"] = "Chi Ti?t Khuy?n M�i S�n";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var donHangs = ViewBag.DonHangs as List<QuanLyBanHangDoAnThucUong.Models.Entities.DonHang>;
    var branchStats = ViewBag.BranchStats as IEnumerable<dynamic>;
}

<div style="margin-left: 250px; margin-top: 20px;" class="pb-5">
    <div class="container-fluid px-4">
        
        <!-- Header -->
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <a href="/Admin/QuanLyKhuyenMai" class="btn btn-link text-decoration-none text-muted p-0 mb-2">
                    <i class="bi bi-arrow-left"></i> Quay l?i Qu?n l� Khuy?n M�i
                </a>
                <h2 class="fw-bold text-dark mb-0">@Model.TenChTrinh</h2>
                <div class="text-muted mt-1">
                    <span class="badge bg-primary me-2">M�: @Model.NoiDungKMai</span>
                    <span class="badge bg-secondary me-2">Gi?m: @Model.PhanTramGiam%</span>
                </div>
            </div>
            <div>
                @if (Model.TrangThaiKMai == "K�ch ho?t" || Model.TrangThaiKMai == "Kích hoạt")
                {
                    <span class="badge bg-success px-3 py-2 rounded-pill fs-6"><i class="bi bi-record-circle"></i> �ang di?n ra</span>
                }
                else
                {
                    <span class="badge bg-secondary px-3 py-2 rounded-pill fs-6"><i class="bi bi-x-circle"></i> �� v� hi?u h�a</span>
                }
            </div>
        </div>

        <div class="row">
            <!-- Summary Stats -->
            <div class="col-md-4 mb-4">
                <div class="card border-0 shadow-sm rounded-4 h-100 bg-white p-4">
                    <h6 class="fw-bold text-muted text-uppercase mb-3">Hi?u su?t m�</h6>
                    <div class="mb-4">
                        <div class="small text-muted mb-1">Lu?t s? d?ng</div>
                        <h2 class="fw-bold text-primary mb-0">@donHangs.Count</h2>
                    </div>
                    <div class="mb-4">
                        <div class="small text-muted mb-1">T?ng ti?n d� gi?m (M s�n ch?u)</div>
                        <h2 class="fw-bold text-danger mb-0">-@donHangs.Sum(d => d.GiamGia).ToString("N0")d</h2>
                    </div>
                    <div>
                        <div class="small text-muted mb-1">Doanh thu mang l?i (GMV)</div>
                        <h2 class="fw-bold text-success mb-0">@donHangs.Sum(d => d.ThanhTienKhachTra).ToString("N0")d</h2>
                    </div>
                </div>
            </div>

            <!-- Top Branches Using Promo -->
            <div class="col-md-8 mb-4">
                <div class="card border-0 shadow-sm rounded-4 h-100 bg-white">
                    <div class="card-header bg-white border-bottom-0 pt-4 pb-2">
                        <h6 class="fw-bold text-dark mb-0">Top Chi nh�nh ph�t sinh don v?i m� n�y</h6>
                    </div>
                    <div class="card-body p-0">
                        <div class="table-responsive">
                            <table class="table table-hover align-middle mb-0">
                                <thead class="bg-light">
                                    <tr>
                                        <th class="ps-4">Chi nh�nh</th>
                                        <th class="text-center">S? don</th>
                                        <th class="text-end pe-4">T?ng gi?m</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @if (!branchStats.Any())
                                    {
                                        <tr>
                                            <td colspan="3" class="text-center py-4 text-muted">Chua c� chi nh�nh n�o �p d?ng</td>
                                        </tr>
                                    }
                                    @foreach (var stat in branchStats.Take(5))
                                    {
                                        <tr>
                                            <td class="ps-4">
                                                <div class="fw-bold text-dark">@stat.GianHang.TenGianHang</div>
                                                <div class="small text-muted">ID: @stat.GianHang.MaGianHang</div>
                                            </td>
                                            <td class="text-center">
                                                <span class="badge bg-primary rounded-pill">@stat.SoDon</span>
                                            </td>
                                            <td class="text-end pe-4 fw-bold text-danger">
                                                -@stat.TongGiam.ToString("N0")d
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Order List -->
        <div class="card border-0 shadow-sm rounded-4 bg-white mt-2 mb-5">
            <div class="card-header bg-white border-bottom-0 pt-4 pb-3">
                <h6 class="fw-bold text-dark mb-0">Danh s�ch �on h�ng chi ti?t</h6>
            </div>
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-hover align-middle mb-0">
                        <thead class="bg-light">
                            <tr>
                                <th class="ps-4">M� �H</th>
                                <th>Kh�ch h�ng</th>
                                <th>Chi nh�nh</th>
                                <th>Ng�y �p d?ng</th>
                                <th>M?c gi?m</th>
                                <th>Kh�ch tr?</th>
                                <th>Tr?ng th�i</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (!donHangs.Any())
                            {
                                <tr>
                                    <td colspan="7" class="text-center py-5 text-muted">Kh�ng c� don h�ng n�o �p d?ng m� n�y</td>
                                </tr>
                            }
                            @foreach (var d in donHangs)
                            {
                                <tr>
                                    <td class="ps-4 fw-bold">#@d.MaDonHang</td>
                                    <td>
                                        <div class="fw-bold">@d.KhachHang?.TenKH</div>
                                    </td>
                                    <td>
                                        <span class="badge bg-light text-dark border"><i class="bi bi-shop text-primary"></i> @d.GianHang?.TenGianHang</span>
                                    </td>
                                    <td>@d.NgayTaoDon.ToString("dd/MM/yyyy HH:mm")</td>
                                    <td class="fw-bold text-danger">-@d.GiamGia.ToString("N0")d</td>
                                    <td class="fw-bold text-success">@d.ThanhTienKhachTra.ToString("N0")d</td>
                                    <td>
                                        @if (d.TrangThaiDonHang == "Ho�n th�nh")
                                        {
                                            <span class="badge bg-success rounded-pill">Ho�n th�nh</span>
                                        }
                                        else if (d.TrangThaiDonHang == "�� h?y")
                                        {
                                            <span class="badge bg-danger rounded-pill">�� h?y</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-warning text-dark rounded-pill">@d.TrangThaiDonHang</span>
                                        }
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

    </div>
</div>


